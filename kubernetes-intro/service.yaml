apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: homework
spec:
  selector:
    podName: homework-pod
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 80
